 <div class="container-fluid">
 <%=form_for(dialect) do |f|%>
<% if @dialect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dialect.errors.count, "error") %> prohibited this dialect from being saved:</h2>

      <ul>
      <% dialect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <form>
       
        
            
                <div class= "form-group">
                    <%=f.label "Select Language"%>
                   
                    <%=f.collection_select :language_id,Language.all,:id,:name,include_blank: false,class:"form-control"%>
                </div>
          
            
            <div class= "form-group">
               
                    <%=f.label "Enter Dialect"%>
                    <%=f.text_field :dialect_name,class:"form-control", id:"language"%>
                
            </div>
           
                <div class= "form-group">
                    <%=f.label "Enter Dialect Description."%>
                  
                 <%=f.text_area :description, :rows => "15",class:"form-control",id:"description"%>
                  
                </div>

                 <div class="actions">
                  <%= f.submit "Submit New Dialect", class: "btn btn-primary" %>
               </div>
        
        
        <%end%>
       
    </form>

</div>